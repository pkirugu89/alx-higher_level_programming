#!/usr/bin/node
// String array
const lang = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Looping through the array and print each line
for (let x = 0; x < lang.length; x++) {
  console.log(lang[x]);
}
