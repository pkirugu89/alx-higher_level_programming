#!/usr/bin/node

// Define the Rectangle class
class Rectangle {
  // class constructor
  constructor (w, h) {
    // initialize instance width n height instance
    this.width = w;
    this.height = h;
  }
}
// Make the Rectangle class visible
module.exports = Rectangle;
